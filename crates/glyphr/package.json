{
  "name": "@typie/glyphr",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/glyphr.js",
  "types": "dist/glyphr.d.ts",
  "scripts": {
    "pack": "wasm-pack build --target web --out-dir dist --no-pack && rm dist/.gitignore"
  }
}
