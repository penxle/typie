package co.typie.editor.interaction.semantics

internal class EditorContextMenuSemantic {
  fun reset() {
    // TODO(editor-parity): port selection and same-cursor context menu presentation state.
  }
}
