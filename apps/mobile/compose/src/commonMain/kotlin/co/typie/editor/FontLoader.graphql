fragment FontLoader_FontFamily on DocumentFontFamily {
  id
  familyName
  source
  fonts {
    id
    weight
    url
    hash
    chunks
  }
}

fragment FontLoader_Document on Document {
  id
  fontFamilies(sources: [DEFAULT, USER, FALLBACK]) {
    ...FontLoader_FontFamily
  }
}
