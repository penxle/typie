query UpdatePasswordScreen_Query {
  me @catch(to: THROW) {
    id
    hasPassword
  }
}

mutation UpdatePasswordScreen_UpdatePassword_Mutation($input: UpdatePasswordInput!) {
  updatePassword(input: $input) {
    id
    hasPassword
  }
}
