package co.typie.editor.interaction.semantics

internal class EditorAutoScrollSemantic {
  fun reset() {
    // TODO(editor-parity): port drag auto-scroll for double-tap drag, long press,
    // and selection-handle drag once those gesture workflows own viewport edges.
  }
}
