query StatsScreen_Query {
  me @catch(to: THROW) {
    id
    name
    documentCount

    usage {
      totalCharacterCount
    }

    characterCountChanges {
      date
      additions
      deletions
    }

    ...ActivityGrid_user
  }
}

mutation StatsScreen_GenerateActivityImage_Mutation {
  generateActivityImage
}
