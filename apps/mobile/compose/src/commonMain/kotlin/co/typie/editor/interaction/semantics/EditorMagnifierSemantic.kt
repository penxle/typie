package co.typie.editor.interaction.semantics

internal class EditorMagnifierSemantic {
  fun reset() {
    // TODO(editor-parity): port long-press and selection-handle magnifier presentation state.
  }
}
