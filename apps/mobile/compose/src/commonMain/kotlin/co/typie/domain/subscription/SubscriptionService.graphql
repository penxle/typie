query SubscriptionService_Query {
  me @catch(to: NULL) {
    id

    subscription {
      id
      state
      startsAt
      expiresAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
