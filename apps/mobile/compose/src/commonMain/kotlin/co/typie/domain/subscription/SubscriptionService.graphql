query SubscriptionService_Query {
  me @catch(to: NULL) {
    id
    entitled
    entitledUntil

    subscription {
      id
      state
      startsAt
      currentPeriodEndsAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
