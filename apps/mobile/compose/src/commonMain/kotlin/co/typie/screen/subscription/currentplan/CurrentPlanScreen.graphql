query CurrentPlanScreen_Query {
  me @catch(to: THROW) {
    id
    credit

    subscription {
      id
      state
      startsAt
      currentPeriodEndsAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
