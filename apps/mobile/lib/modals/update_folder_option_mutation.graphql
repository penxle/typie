mutation ShareFolder_UpdateFolderOption_Mutation($input: UpdateFolderOptionInput!) {
  updateFolderOption(input: $input) {
    id

    entity {
      id
      visibility

      children {
        id
        visibility
      }
    }
  }
}