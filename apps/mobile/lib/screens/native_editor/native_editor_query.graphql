query NativeEditorScreen_Query($slug: String!) {
  entity(slug: $slug) {
    id
    slug

    node {
      __typename

      ... on Document {
        id
        title
        nullableTitle
        subtitle
        snapshot
      }
    }
  }
}
