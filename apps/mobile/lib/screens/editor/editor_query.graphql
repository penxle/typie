query EditorScreen_Query($slug: String!, $siteId: ID!) {
  me {
    id

    fontFamilies {
      id
      name

      fonts {
        id
        weight
      }
    }

    subscription {
      id
    }
  }

  post(slug: $slug) {
    id
    title
    createdAt
    updatedAt

    entity {
      id
      visibility
      slug
      url

      site {
        id
      }
    }

    characterCountChange {
      additions
      deletions
    }
  }

  site(siteId: $siteId) {
    id

    templates {
      id
      title

      entity {
        id
        slug
      }
    }
  }
}
