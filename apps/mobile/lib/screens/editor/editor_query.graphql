query EditorScreen_Query($slug: String!) {
  post(slug: $slug) {
    id
    title

    entity {
      id
      visibility
      url

      site {
        id

        fonts {
          id
          name
        }
      }
    }
  }
}