query EditorScreen_Query($slug: String!) {
  post(slug: $slug) {
    id
    title

    entity {
      id
      visibility

      site {
        id

        fonts {
          id
          name
        }
      }
    }
  }
}