query EditorScreen_Query($slug: String!) {
  post(slug: $slug) {
    id
    title

    entity {
      id
      visibility
    }
  }
}

mutation EditorScreen_DeletePost_Mutation($input: DeletePostInput!) {
  deletePost(input: $input) {
    id
  }
}