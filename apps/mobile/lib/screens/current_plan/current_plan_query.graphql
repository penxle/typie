query CurrentPlanScreen_Query {
  me {
    id
    credit

    subscription {
      id
      state
      startsAt
      expiresAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
