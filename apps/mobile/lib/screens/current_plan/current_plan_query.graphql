query CurrentPlanScreen_Query {
  me {
    id
    credit

    subscription {
      id
      startsAt
      expiresAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
