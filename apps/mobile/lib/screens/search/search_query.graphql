query SearchScreen_Query($siteId: ID!, $query: String!) {
  search(siteId: $siteId, query: $query) {
    hits {
      ... on SearchHitDocument {
        title
        text

        document {
          id
          type
          updatedAt

          entity {
            id
            slug
          }
        }
      }
    }
  }
}
