query SiteSettingsScreen_Query($siteId: ID!) {
  me {
    id

    subscription {
      id
    }

    sites {
      id
    }
  }

  site(siteId: $siteId) {
    id
    name
    slug
    dateDisplay
    documentCount
    folderCount

    logo {
      id
      url
    }
  }
}
