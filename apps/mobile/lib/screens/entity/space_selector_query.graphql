query SpaceSelector_Query {
  me {
    id

    subscription {
      id
    }

    sites {
      id
      name
      documentCount
      folderCount

      logo {
        id
        url
      }
    }
  }
}
