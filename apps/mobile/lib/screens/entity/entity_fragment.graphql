fragment EntityScreen_Entity_entity on Entity {
  id
  depth
  slug
  order
  url
  visibility
  availability

  node {
    ... on Folder {
      id
      name
      maxDescendantFoldersDepth
      characterCount
      folderCount
      postCount
    }

    ... on Post {
      id
      title
      type
      excerpt
      updatedAt
      characterCount

      characterCountChange {
        additions
        deletions
      }
    }

    ... on Document {
      id
      title
      excerpt
      updatedAt

      characterCountChange {
        additions
        deletions
      }
    }
  }

  ancestors {
    id

    node {
      ... on Folder {
        id
        name
      }
    }
  }
}
