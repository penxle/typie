fragment EntityScreen_Entity_entity on Entity {
  id
  slug
  order

  node {
    ... on Folder {
      id
      name
    }

    ... on Post {
      id
      title
      excerpt
      updatedAt
    }
  }
}