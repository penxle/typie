fragment EntityScreen_Entity_entity on Entity {
  id
  depth
  slug
  order
  url

  node {
    ... on Folder {
      id
      name
      maxDescendantFoldersDepth
    }

    ... on Post {
      id
      title
      excerpt
      updatedAt
    }
  }

  ancestors {
    id

    node {
      ... on Folder {
        id
        name
      }
    }
  }
}