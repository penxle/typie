{
  "name": "@typie/literoom",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown && node scripts/package-function.ts",
    "build:layers": "node scripts/build-layers.ts",
    "lint:eslint": "eslint --max-warnings 0 .",
    "lint:prettier": "prettier --check --ignore-unknown --ignore-path ../../.gitignore --ignore-path ../../.prettierignore '**/*'",
    "lint:spellcheck": "cspell --no-progress --no-must-find-files --relative '**/*'",
    "lint:typecheck": "tsc"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1121.0",
    "@typie/tsconfig": "workspace:*",
    "execa": "^10.0.1",
    "sharp": "^0.35.4",
    "tsdown": "^0.22.14",
    "typescript": "^6.0.3"
  }
}
