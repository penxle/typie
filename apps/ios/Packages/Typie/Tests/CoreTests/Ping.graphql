query Ping_Query {
  me {
    id
  }
}

subscription Ping_Subscription {
  userGoalUpdateStream {
    id
  }
}

subscription Ping_Usage_Subscription($userId: ID!) {
  userUsageUpdateStream(userId: $userId) {
    id
  }
}
