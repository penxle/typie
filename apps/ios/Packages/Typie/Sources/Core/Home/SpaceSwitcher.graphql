query SpaceSwitcher_Query {
  me {
    id
    sites {
      id
      name
      url
      logo {
        ...TImage_image
      }
    }
  }
}

mutation SpaceSwitcher_CreateSite_Mutation($input: CreateSiteInput!) {
  createSite(input: $input) {
    id
  }
}
