query SiteSwitcher_Query {
  me {
    id
    sites {
      id
      name
      url
      logo {
        id
        ...Img_image
      }
    }
  }
}

mutation SiteSwitcher_CreateSite_Mutation($input: CreateSiteInput!) {
  createSite(input: $input) {
    id
  }
}
