query FolderContents_Query($entityId: ID!) {
  entity(entityId: $entityId) {
    id
    ...EntityRow_entity

    node {
      __typename

      ... on Folder {
        id
        characterCount
      }
    }

    children {
      id
      ...EntityRow_entity
    }
  }
}
