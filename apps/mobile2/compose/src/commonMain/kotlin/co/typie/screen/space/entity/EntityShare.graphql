query EntityShare_Query($entityIds: [ID!]!) {
  entities(entityIds: $entityIds) {
    id
    type
    url
    visibility

    node {
      __typename

      ... on Folder {
        id
        name

        thumbnail {
          id
          url
        }
      }

      ... on Document {
        id
        title
        password
        contentRating
        allowReaction
        protectContent

        thumbnail {
          id
          url
        }
      }
    }
  }
}
