query FolderScreen_Query($entityId: ID!) {
  entity(entityId: $entityId) @catch(to: THROW) {
    ...EntityDetails_entity

    site {
      id
      name
    }

    children {
      ...EntityRow_entity
    }
  }
}
