query MainDrawer_Query {
  me @catch(to: THROW) {
    id
    name
    email

    avatar {
      id
      ...Img_image
    }

    sites {
      id
      name
      url

      logo {
        id
        ...Img_image
      }
    }
  }
}

mutation MainDrawer_CreateSite_Mutation($input: CreateSiteInput!) {
  createSite(input: $input) {
    id
    name

    logo {
      id
      ...Img_image
    }
  }
}
