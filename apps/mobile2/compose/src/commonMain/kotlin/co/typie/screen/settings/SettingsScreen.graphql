query SettingsScreen_Query {
  me @catch(to: THROW) {
    id
    marketingConsent
    hasPassword
  }
}

mutation SettingsScreen_UpdateMarketingConsent_Mutation($input: UpdateMarketingConsentInput!) {
  updateMarketingConsent(input: $input) {
    id
    marketingConsent
  }
}
