query SubscriptionService_Query {
  me @catch(to: THROW) {
    id

    subscription {
      id
      state
      startsAt
      expiresAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
