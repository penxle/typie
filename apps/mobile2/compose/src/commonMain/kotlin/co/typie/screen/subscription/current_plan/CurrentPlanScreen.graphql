query CurrentPlanScreen_Query {
  me @catch(to: THROW) {
    id
    credit

    subscription {
      id
      state
      startsAt
      expiresAt

      plan {
        id
        name
        fee
        availability
      }
    }
  }
}
