{
  "name": "@typie/prism",
  "version": "0.0.1",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint:eslint": "eslint --max-warnings 0 .",
    "lint:prettier": "prettier --check --ignore-unknown --ignore-path ../../.gitignore --ignore-path ../../.prettierignore '**/*'",
    "lint:spellcheck": "cspell --no-progress --no-must-find-files --relative '**/*'",
    "lint:typecheck": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@typie/editor-ffi": "workspace:*",
    "ts-pattern": "^5.9.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@typie/tsconfig": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
