{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "AWS_PROFILE"],
  "tasks": {
    "codegen": {
      "dependsOn": ["^build"],
      "outputs": [".svelte-kit/**", ".sark/**", "styled-system/**"]
    },
    "build": {
      "dependsOn": ["codegen", "^build"],
      "outputs": ["dist/**"]
    },
    "wasm:build": {
      "outputs": ["pkg/**"]
    },
    "dev": {
      "dependsOn": ["codegen"],
      "persistent": true,
      "cache": false
    },
    "lint:svelte": {
      "dependsOn": ["^codegen", "codegen", "^wasm:build"]
    },
    "lint:typecheck": {
      "dependsOn": ["codegen", "^wasm:build"]
    },
    "test": {
      "dependsOn": ["codegen"],
      "outputs": ["coverage/**"]
    }
  }
}
