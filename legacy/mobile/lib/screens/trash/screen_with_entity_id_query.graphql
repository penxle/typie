# import "./entity_fragment.graphql"

query TrashScreen_WithEntityId_Query($entityId: ID!) {
  entity(entityId: $entityId) {
    id
    site {
      id
      name
    }
    ...TrashScreen_Entity_entity

    deletedChildren {
      id
      ...TrashScreen_Entity_entity
    }
  }
}
